# M3 마무리 — TX 상태머신 테스트 실행

> Block C — VASP 연동 + 복구 + 멀티체인 추상화 · M3 마무리 · 15분  
> 위치: day06.md S22 완료 기준 직후

---

## 0. 왜 컴포넌트 테스트인가

M3에서 구현한 것들은 TX 상태머신 레이어에 집중돼 있다.

```
S13  TxStateMachine — 상태 전이 규칙
S14  IBlockchainAdapter — 멀티체인 추상화
S15  EVMAdapter 구현
S16  Idempotency Guard
S17  retryWithBackoff
S18  handleTxRevert
S19~20  handleTimeout / handleReorg
S21  이중 채널 아키텍처
S22  pollStaleRequests
```

이 레이어를 검증하는 데 전체 서비스 스택(Redis, 블록체인, WebhookServer)은 필요 없다. `TxStateMachineService` + `PgTxRepository` + 실제 PostgreSQL만 올리면 충분하다.

전체 스택 통합테스트(mock-VASP, Sepolia)는 M3를 넘어 전체 파이프라인이 완성된 이후에 의미가 있다.

---

## 1. `tx-status.integration.test.ts` 구조

VASP는 `ControlledVaspClient` (테스트 전용 페이크)로 교체. 블록체인 없이 실행된다.

```
TxStateMachineService
       │
       ├── PgTxRepository  ← 실제 PostgreSQL
       │
       └── ControlledVaspClient  ← 페이크 (응답 직접 제어)
```

| 테스트 | 검증 항목 | M3 연결 |
|---|---|---|
| `[2]` SUBMITTED → MINED → CONFIRMED → FINALIZED | 정상 전이 순서 | S13 |
| `[3]` MINED → REORGED → MINED | REORG 복구 | S19~20 |
| `[4]` SUBMITTED → FAILED | REVERT | S18 |
| `[5]` FINALIZED 이후 전이 → InvalidStatusTransitionError | 종단 상태 보호 | S13 |
| `[6]` FAILED 이후 전이 → DB 변경 없음 | 종단 상태 보호 | S13 |
| `[7]` PENDING 30분 초과 → pollStaleRequests → CONFIRMED | 폴링 복구 | S22 |
| `[8]` transition 이벤트 발행 확인 | 이벤트 설계 | S13 |

---

## 2. 실습

```bash
npx jest tx-status --runInBand
```

통과 후 확인할 것:

**`[5]` 종단 상태 보호**
```
InvalidStatusTransitionError: FINALIZED → FAILED is not allowed
```
FINALIZED에서는 어떤 전이도 불가능하다. VALID_TRANSITIONS 맵에서 FINALIZED 키가 빈 배열이어야 한다.

**`[7]` pollStaleRequests**
```
{ processed: 1 }
```
30분 초과 PENDING 건을 VASP에 직접 조회해 CONFIRMED로 전이했다는 뜻. 콜백 없이도 복구된다.

---

## 3. 컴포넌트 테스트의 가치

통합 테스트(전체 스택)에서 실패했을 때, 원인이 여기서 먼저 잡히면 "TX 상태머신 자체 버그"다. 여기서 통과하고 통합 테스트에서만 실패하면 "컴포넌트 조합 시 발생하는 버그"다. 디버깅 범위가 좁혀진다.

---

## 완료 기준

- [ ] `tx-status` 8개 전체 통과

---

## 다음

M4 S23 — 내부 원장 설계.  
M3에서 CONFIRMED 전이가 발생하면 `LedgerService`가 `user_nft_holdings`를 어떻게 갱신하는가.
